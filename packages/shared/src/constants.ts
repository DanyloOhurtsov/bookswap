/**
 * §8: усі ендпоінти живуть під цим префіксом.
 *
 * Єдине джерело правди для обох застосунків: `apps/api` віддає його в
 * `setGlobalPrefix`, `apps/web` будує з нього URL запитів. Дублювання рядка
 * '/api/v1' у двох місцях розсинхронізується при першій же зміні версії.
 */
export const API_PREFIX = '/api/v1'

/**
 * §6.1: ім'я сесійної кукі.
 *
 * Тут, а не лише в `apps/api`, з тієї самої причини, що й `API_PREFIX`: відколи
 * `apps/web` рендерить захищену сторінку на сервері, він мусить сам дістати цю
 * кукі із запиту й прокинути її в API — а імпортувати з `apps/api` йому не
 * можна (§10). Другий рядковий літерал розійшовся б із першим при першому ж
 * перейменуванні, і зламалася б рівно автентифікація.
 *
 * Префікс `__Host-` навмисно не використовується: він вимагає `Secure`, а в dev
 * фронт і API ходять по http://localhost.
 */
export const SESSION_COOKIE_NAME = 'bookswap_session'
