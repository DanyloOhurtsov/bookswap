# BookSwap

Джерело вимог: docs/specification.md. При суперечності код ↔ спека — спека головна,
але руйнівні зміни не роби без пояснення.

## Домен
- Модель Work → Translation → Edition → Copy не спрощувати.
- Позичається тільки Copy.

## Стек і код
- Без Redis, JWT, GraphQL, мікросервісів.
- Стек не міняти без доказу несумісності.
- `any` — тільки з обґрунтуванням. Помилки TS/ESLint/тестів не глушити.
- Усі API під /api/v1, помилки з машиночитним `code`.
- Спільні zod-схеми й контракти — у packages/shared. DTO Nest — runtime-валідація.
- Без абстракцій «на майбутнє».

## Робочий процес
- Гілка feat/stage-7-catalog-quality. Перевір `git branch --show-current`;
  якщо інша — зупинись і повідом, нічого не роби.
- Ніяких commit, push, merge, rebase, stash.
- Не чіпай docs/specification.md.
- Не починай роботу наступних підетапів.
- Перевірка — `./gate.sh`, поки не зелений. 